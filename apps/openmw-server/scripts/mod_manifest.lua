-- Required client content for this server.
--
-- Release packages intentionally ship an empty manifest so they do not impose
-- one server operator's mod list on every installation. Populate this table,
-- or replace this file with generated output, when the server should require
-- exact client content filenames and SHA-256 hashes.
--
-- Example:
-- return {
--     { filename = "Morrowind.esm", sha256 = "<64-character SHA-256>" },
-- }

return {}
